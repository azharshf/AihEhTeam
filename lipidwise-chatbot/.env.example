# Get your free API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Free models on OpenRouter change often — check https://openrouter.ai/models?max_price=0
# for what's currently live. rag.py also tries FALLBACK_MODELS in order if
# the primary one is rate-limited (common on the shared free tier).
LLM_MODEL=google/gemma-4-26b-a4b-it:free
